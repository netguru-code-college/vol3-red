<div class="container">

  <div class="col">
    <div class="jumbotron mt-4 text-center">
      <h2>Hello <b><%= current_user.name %> <%= current_user.surname %></b> </h2>
      <p>Duis aute irure dolor in reprehenderit in voluptate velit esse
        cillum dolore eu fugiat nulla pariatur. </p>
        <%= link_to 'Add a new estate', new_dashboard_estate_path, class: "btn btn-lg btn-outline-info mt-2" %>
      </div>
    <%= render "notification" %>
  </div>

    <!-- if current_user = admin show Estate.all -->
    <!-- in current_user != admin ahow current_user.apartment -->
    <!-- if admin?(@user) -->
    <% if true %>
      <h3 id="list_estates"> List of estates </h3>
      <%= render "estates_all" %>
    <% else %>
      <%= render "my_apartments" %>
    <% end %>
    <!-- end show this if current_user = admin -->
</div>
